{{#each comments}}
<div class="card">
    <div class="card-header">
        Comment Submitted on {{formatDate this.createdAt}}
    </div>
    <div class="card-block">
        <blockquote class="card-blockquote">
            <p>{{this.comment}}</p>
        </blockquote>
    </div>
</div>
{{/each}}